# Database

Base de datos PostgreSQL y migraciones con Liquibase.

## Conexion esperada

- PostgreSQL: `localhost:5437`
- Base: `mini_proyect`
- Usuario: `postgres`

## Arranque

Desde la raiz de este repo:

```powershell
docker compose up -d
```
